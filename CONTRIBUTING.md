# Contributing

Use a feature branch, preserve the MIT license, keep product/test code Rust-only, add boundary and adversarial tests, run formatting/Clippy/tests/docs/release/evidence verification, and never commit wallets, databases, validator ledgers, `.env`, tokens, or participant metadata. Changes that add arbitrary instructions, generalized CPI, custody, mixing, manipulation, or unsupported privacy claims are out of scope.
